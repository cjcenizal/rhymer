{
  "name": "rhymer",
  "dependencies": {
    "classnames": "2.1.1",
    "flux": "2.0.3",
    "jquery": "2.1.3",
    "keymirror": "0.1.1",
    "object-assign": "2.0.0",
    "underscore": "1.8.3",
    "q": "1.3.0",
    "react": "0.13.3",
    "reactify": "1.1.0",
    "medium-editor": "^4.10.1"
  },
  "scripts": {
    "clean": "rm -f -R build && mkdir build && mkdir -p build/{styles,scripts}",
    "css": "sass src/main.scss build/styles/main.css && postcss --use autoprefixer -o build/styles/main.css build/styles/main.css",
    "html": "jade src/index.jade --out build --pretty",
    "js": "browserify -t reactify src/main.js -o build/scripts/main.js",
    "build": "npm run clean && npm run js && npm run css && npm run html",
    "serve": "ws -d build"
  }
}
